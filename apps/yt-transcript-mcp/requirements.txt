yt-dlp>=2026.7.4
# The server uses ONLY the Python standard library + yt-dlp (subprocess).
# No `mcp` SDK: it needs pydantic-core (Rust), which won't build on Termux/Android,
# so server.py hand-rolls the MCP Streamable-HTTP slice it needs. Nothing else to install.
# Optional: enables yt-dlp --impersonate for extra robustness against YouTube blocks.
# Not required (fetch worked without it in testing), but cheap insurance on the phone:
# curl_cffi>=0.7
